let a = true;  // == let a: bool = true - Rust is statically typed, but it also implicitly apply a proper type for the variable if you didnt specify
let (a, b) = (1, 2)

let mut z = 5;


const N: i32 = 5;   // Const needs to be explicitly specified before compile-time
static N: i32 = 5;   // Static also need to be explicitly specified before compile-time

